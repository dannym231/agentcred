[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "agentcred"
version = "0.1.0"
description = "Local SDK for AI agent identity, wallet, and reputation"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["agentcred*"]
